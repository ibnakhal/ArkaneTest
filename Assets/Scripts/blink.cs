using UnityEngine;
using System.Collections;

public class blink : MonoBehaviour {
    [SerializeField]
    private GameObject indicator;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	    if(Input.GetButtonDown("Mouse 1")
            {
            Ray teleRay = RaycastHit ( )
        }
	}
}
